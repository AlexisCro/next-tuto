import { Inter } from 'next/font/google';
import { Lexend_Giga } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const lexendGiga = Lexend_Giga({ subsets: ['latin'] });